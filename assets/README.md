Place the supplied first-view images here before publishing:

- `first-view-desktop.png`
- `first-view-mobile.png`

The HTML uses these files directly. If they are missing, the CSS fallback first-view remains visible.
